<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.penocean.ehs.mapper.CodeMasterMapper">

  <resultMap id="CodeMasterResultMap" type="com.penocean.ehs.model.CodeMaster">
    <id property="id" column="id"/>
    <result property="groupCode" column="group_code"/>
    <result property="code" column="code"/>
    <result property="name" column="name"/>
    <result property="description" column="description"/>
    <result property="sortOrder" column="sort_order"/>
    <result property="active" column="active"/>
    <result property="createdAt" column="created_at"/>
    <result property="updatedAt" column="updated_at"/>
    <result property="deleted" column="deleted"/>
  </resultMap>

  <select id="findByGroup" resultMap="CodeMasterResultMap">
    SELECT id, group_code, code, name, description, sort_order, active,
           created_at, updated_at, deleted
      FROM tb_code
     WHERE group_code = #{groupCode} AND deleted = 0
    <if test="activeOnly != null and activeOnly">
      AND active = 1
    </if>
     ORDER BY sort_order ASC, id ASC
  </select>

  <select id="findById" resultMap="CodeMasterResultMap">
    SELECT id, group_code, code, name, description, sort_order, active,
           created_at, updated_at, deleted
      FROM tb_code
     WHERE id = #{id} AND deleted = 0
  </select>

  <select id="findByGroupAndCode" resultMap="CodeMasterResultMap">
    SELECT id, group_code, code, name, description, sort_order, active,
           created_at, updated_at, deleted
      FROM tb_code
     WHERE group_code = #{groupCode} AND code = #{code} AND deleted = 0
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO tb_code (group_code, code, name, description, sort_order, active, created_at, deleted)
    VALUES (#{groupCode}, #{code}, #{name}, #{description},
            <choose><when test="sortOrder != null">#{sortOrder}</when><otherwise>0</otherwise></choose>,
            <choose><when test="active != null">#{active}</when><otherwise>1</otherwise></choose>,
            SYSUTCDATETIME(), 0)
  </insert>

  <update id="update">
    UPDATE tb_code
       SET name = #{name},
           description = #{description},
           sort_order = <choose><when test="sortOrder != null">#{sortOrder}</when><otherwise>sort_order</otherwise></choose>,
           active = <choose><when test="active != null">#{active}</when><otherwise>active</otherwise></choose>,
           updated_at = SYSUTCDATETIME()
     WHERE id = #{id}
  </update>

  <update id="softDelete">
    UPDATE tb_code
       SET deleted = 1, updated_at = SYSUTCDATETIME()
     WHERE id = #{id}
  </update>

</mapper>
