<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.penocean.ehs.mapper.NotificationMapper">

  <resultMap id="NotificationResultMap" type="com.penocean.ehs.model.Notification">
    <id property="id" column="id"/>
    <result property="recipientUserId" column="recipient_user_id"/>
    <result property="channel" column="channel"/>
    <result property="subject" column="subject"/>
    <result property="body" column="body"/>
    <result property="status" column="status"/>
    <result property="sentAt" column="sent_at"/>
    <result property="createdAt" column="created_at"/>
    <result property="deleted" column="deleted"/>
  </resultMap>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO tb_notification (
      recipient_user_id, channel, subject, body, status, sent_at, created_at, deleted
    ) VALUES (
      #{recipientUserId,jdbcType=BIGINT}, #{channel}, #{subject}, #{body},
      <choose>
        <when test="status != null and status != ''">#{status}</when>
        <otherwise>'PENDING'</otherwise>
      </choose>,
      #{sentAt,jdbcType=TIMESTAMP}, SYSUTCDATETIME(), 0
    )
  </insert>

  <select id="findByUser" resultMap="NotificationResultMap">
    SELECT id, recipient_user_id, channel, subject, body, status, sent_at, created_at, deleted
    FROM tb_notification
    WHERE recipient_user_id = #{userId} AND deleted = 0
    ORDER BY created_at DESC
    OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
  </select>

  <select id="countUnreadByUser" resultType="long">
    SELECT COUNT(1)
    FROM tb_notification
    WHERE recipient_user_id = #{userId}
      AND status = 'PENDING'
      AND deleted = 0
  </select>

</mapper>
